## 1. 状态模型与输入路由

- [x] 1.1 在 `MobileFactoryDemo` 中引入显式编辑会话状态，并保留进入编辑前的世界控制模式用于退出恢复
- [x] 1.2 重构移动工厂 demo 的模式切换入口，使玩家控制、工厂控制、部署预览、观察模式与编辑会话的切换关系可单独表达
- [x] 1.3 将输入焦点路由升级为“世界视图 / 编辑视口 / 编辑操作面板”三段式，避免当前双 pane 逻辑误吞输入

## 2. HUD 主体重构

- [x] 2.1 按 `FactoryDemo` 的交互层级重构 `MobileFactoryHud` 主外壳，保留顶部 workspace chrome 并收敛为单主信息面板
- [x] 2.2 重新整理移动工厂 demo 的 workspace 内容分配，让命令、蓝图、存档、验证、详情等宏观内容回到主面板体系
- [x] 2.3 统一移动工厂 demo 的主状态文案、模式标签和按钮反馈，使 HUD 能同时表达世界控制状态与编辑会话状态

## 3. 独立编辑操作面板

- [x] 3.1 为移动工厂编辑模式实现独立的 `EditorOperationPanel` 容器与打开/关闭生命周期
- [x] 3.2 将内部建造模式、工具选择、朝向旋转、删除操作、蓝图快捷入口、选中摘要和焦点提示迁入编辑操作面板
- [x] 3.3 调整编辑模式下的详情窗口、工作区切换和操作面板共存关系，确保彼此职责不冲突

## 4. 回归验证与体验收口

- [x] 4.1 更新 focused mobile demo 的 smoke test，覆盖新 HUD 外壳、编辑面板打开/关闭与 workspace 保持行为
- [x] 4.2 增加编辑模式退出后恢复原世界控制模式的验证，覆盖玩家控制、工厂控制、部署预览和观察模式
- [x] 4.3 收口热键提示、按钮命名和引导文案，确保玩家能够理解“工作区切换”和“编辑模式切换”的区别
